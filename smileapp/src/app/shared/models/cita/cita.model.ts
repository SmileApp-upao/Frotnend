export interface citaModel {
   dentistId:number; //ide del usuario dentista
    reason: string;
    date: string;  // La fecha se manejará como string en formato 'YYYY-MM-DD'
    hour: string;  // La hora se manejará como string en formato 'HH:mm'
  }